/**
 * Attaches function func to the window object's load event
 *
 * @param  {Function} func The function to evoke upon the window's load event
 * @return {void}
 *
 * @category Utility
 */
const addLoadEvent = func => {
  //  TODO
}

export default addLoadEvent
