/**
 * Inserts element _node_  after _referenceNode_ that is contained by _parent_ element,
 *
 *
 * @param {HTMLElement} parent The root node containing the reference node
 * @param {HTMLElement} node The node to insert after referenceNode
 * @param {HTMLElement} referenceNode The node preceding the node to insert
 * @return {void}
 */
const insertAfter = (parent, node, referenceNode) => {
  //  TODO
}

export default insertAfter
