/**
 * Sets the expiration date for cookie _name_ in the past
 *
 * @param {string} name The name of the cookie to expire
 * @return {void}
 */
const deleteCookie = name => {
  //  TODO
}

export default deleteCookie
