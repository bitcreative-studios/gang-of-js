/**
 * Creates a copy of _object_. Used in prototypal inheritance.
 *
 * @param {Object} object The object whose properties to copy
 * @return {Object}
 */
const clone = object => {
  //  TODO
}

export default clone
